# if self.speed > 1 or self.speed < 0:
#     raise ValueError("Should be a number between 0 and 1")
# if self.endurance > 1 or self.endurance < 0:
#     raise ValueError("Should be a number between 0 and 1")
# if self.accuracy > 1 or self.accuracy < 0:
#     raise ValueError("Should be a number between 0 and 1")

# if not (self.speed or self.endurance or self.accuracy) >= 0 and (self.speed or self.endurance or self.accuracy) <= 1:
#     raise ValueError("Should be a number between 0 and 1")

# idee Dominique Pendjol
# if ((self.speed > 1 or self.speed < 0) or (self.endurance > 1 or self.endurance < 0) or (self.accuracy > 1 or self.accuracy < 0)):
#     raise ValueError("Should be a number between 0 and 1")

# if not ((self.speed > 0 or self.speed < 1) and (self.endurance > 0 or self.endurance < 1) and (self.accuracy > 0 or self.accuracy < 1)):
#     raise ValueError("Should be a number between 0 and 1")

# idee Stephanie Michielsen
